// this is step definistion file which will have the code

const assert = require('assert');
const { Given, When, Then } = require('@cucumber/cucumber');

Given('user logs to site with {username} and {password}', function (string, string2) {
           // Write code here that turns the phrase above into concrete actions
           return 'pending';
         });